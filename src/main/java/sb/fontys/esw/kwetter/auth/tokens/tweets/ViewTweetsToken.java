package sb.fontys.esw.kwetter.auth.tokens.tweets;

import sb.fontys.esw.kwetter.auth.tokens.Nothing;
import sb.fontys.esw.kwetter.auth.tokens.Token;


public class ViewTweetsToken extends Token<Nothing> {

    public ViewTweetsToken(Nothing data) {
        super(data);
    }
}
